import { StyleSheet } from "react-native";

const SChatMain = StyleSheet.create({
  container: {
    flexDirection: 'column',
    flex: 1,
  },
  flexParent: {
    flex: 1,
  }
});

export default SChatMain;