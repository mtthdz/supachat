import { StyleSheet } from "react-native";

const SChatTimestamp = StyleSheet.create({
  container: {
    marginTop: 3,
    marginBottom: 6,
    flexDirection: 'row',
    flexWrap: 'nowrap',
  },
  text: {
    marginLeft: 6,
    alignSelf: 'center',
    fontSize: 12,
    color: 'grey',
  }
});

export default SChatTimestamp;