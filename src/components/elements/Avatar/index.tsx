import { Image, View } from "react-native";
import SAvatar from "./Avatar.styles";

/**
 * TODO: add variants to component
 * 
 * @returns JSX
 */
const Avatar = () => {
  return (
    <View style={SAvatar.view}>
    </View>
  );
}

export default Avatar;