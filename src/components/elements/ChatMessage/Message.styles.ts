import { StyleSheet } from "react-native";

const SMessage = StyleSheet.create({
  view: {
    maxWidth: '60%',
    backgroundColor: '#efefef',
    padding: 7,
    marginVertical: 3,
    borderRadius: 15,
    alignSelf: 'flex-start',
  },
  text: {
    color: '#000000',
  }
})

export default SMessage;
