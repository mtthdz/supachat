import { StyleSheet } from "react-native";

const SChatBody = StyleSheet.create({
  container: {
    // flex: 1,
    backgroundColor: 'lightgrey',
    // height: 400,
  }
});

export default SChatBody;